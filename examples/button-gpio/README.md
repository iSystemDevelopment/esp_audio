# DIY GPIO button

Original MIT helper (`AudioButtonGpio`). Debounced active-low input.

You pick the pin. This is **not** the Octopus PRO encoder / SCALE / OC / TRIGGER map.

```bash
# Arduino: open this sketch after adding the EspAudio library folder.
```
