# Octopus PWA

Simplified live harp for EspAudio.

```bash
python -m http.server 8765
```

http://127.0.0.1:8765/ → **Start audio** → click strings or keys 1–8.

D-BEAM slider = **MIDI CC1 Modulation** (vibrato only). Digital = beam trigger. Spec: [../../docs/OCTOPUS.md](../../docs/OCTOPUS.md).
